#str1 = "This is my string learning course from\nlearning with apna college youtube channel" # \n is used for new line.
#print(len(str1)) # to find the length of string
#print(str1)

#concatenation of string
str1 = "Pragya"
str2 = "Rai"
str = str1 + " " + str2 # + is used to concatenate two strings
print(len(str)) # to find length of concatenated string
print(str) # to print concatenated string
ch = str[4] # to know character at index 4
print(ch) # to print character at index 4
